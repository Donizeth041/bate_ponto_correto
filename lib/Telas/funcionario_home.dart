import 'package:flutter/material.dart';

class FuncionarioHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tela do Funcionário")),
      body: Center(child: Text("Bem-vindo à Tela do Funcionário!")),
    );
  }
}
